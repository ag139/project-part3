apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: {{ .Values.namespace }}
  labels: { app: devops-app, component: backend }
spec:
  type: ClusterIP
  selector: { app: devops-app, component: backend }
  ports:
    - name: http
      port: 5000
      targetPort: http
