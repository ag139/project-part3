apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: {{ .Values.namespace }}
  labels: { app: devops-app, component: frontend }
spec:
  type: ClusterIP
  selector: { app: devops-app, component: frontend }
  ports:
    - name: http
      port: 80
      targetPort: http
