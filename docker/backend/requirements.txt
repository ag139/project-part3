# Versions pinned to what was tested and verified in this deployment.
# Unpinned dependencies mean a rebuild months from now can pull a different
# release and break the image without any code change - the same reasoning
# behind never using the "latest" image tag.
Flask==3.1.3
psycopg2-binary==2.9.12
boto3==1.43.61
